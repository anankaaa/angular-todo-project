import { ToolTipSingletonDirective } from './tool-tip-singleton.directive';

describe('ToolTipSingletonDirective', () => {
  it('should create an instance', () => {
    const directive = new ToolTipSingletonDirective();
    expect(directive).toBeTruthy();
  });
});
